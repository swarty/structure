// import gulp from 'gulp';
// import config from '../config.js';


// gulp.task('folders', () => gulp
//   .src(
// 		[
// 			config.src.root + '/*.*',

// 		])
//   .pipe(gulp.dest(config.dest.root))
// );

// const build = gulp => gulp.series('folders');
// const watch = gulp => () => gulp.watch(config.src.folders + '/*', gulp.parallel('folders'));

// module.exports.build = build;
// module.exports.watch = watch;